# Repositories

ASP.NET MVC equivalent: `Repositories/`.

Use this folder for repository classes that encapsulate persistence concerns.
`app/Models` currently contains repository-style classes and can be migrated here incrementally.
