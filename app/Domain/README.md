# Domain

ASP.NET MVC equivalent: `Domain/`.

Use this folder for business-domain objects and rules that should not depend on HTTP or persistence details.
`app/Entities` currently serves this purpose and can be merged into this folder in phases.
