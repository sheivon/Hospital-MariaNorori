# Infrastructure

ASP.NET MVC equivalent: `Infrastructure/`.

Use this folder for cross-cutting technical components (logging, cache adapters, external clients, file storage, framework integration).
Current low-level components still live in `app/Core` and can be moved here gradually.
