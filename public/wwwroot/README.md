# wwwroot

ASP.NET MVC equivalent static-content root.

This folder is the target location for static files (css/js/images/vendor).
Current static assets are still served from `public/assets` for compatibility.
